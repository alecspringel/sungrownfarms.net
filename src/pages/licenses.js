import React from 'react';
import Button from '../components/Button';

const licenses = () => (
  <div className="text-center section-p-t">
    <p className="m-b20">Licenses Page Placeholder</p>
    <Button to="/">Back to Home</Button>
  </div>
);

export default licenses;
